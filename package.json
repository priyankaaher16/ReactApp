{
	"name": "myreact",
	"version": "0.1.0",
	"private": true,
	"dependencies": {
		"i": "^0.3.6",
		"pm2": "^3.5.0",
		"react": "^16.8.6",
		"react-dom": "^16.8.6",
		"react-scripts": "2.1.8"
	},
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
		"deploy": "npm run build && pm2 delete pm.config.json && pm2 start pm.config.json"
	},
	"eslintConfig": {
		"extends": "react-app"
	},
	"browserslist": [
		">0.2%",
		"not dead",
		"not ie <= 11",
		"not op_mini all"
	]
}
